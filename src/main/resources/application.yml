hello: "C'est la prod"

spring:
  profiles: prod

server:
  port: 8080

mysql:
  url: ${APP_URL}
  username: ${APP_USERNAME}
  password: ${APP_PASSWORD}

---
hello: "C'est le mode dev"

spring:
  profiles: default

server:
  port: 8080

mysql:
  url: "jdbc:mysql://captain.javarover.wilders.dev:33306/portfolio?serverTimezone=GMT"
  username: portfolio_root
  password: portfolio_password